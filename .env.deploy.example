# Copie para .env.deploy — Hostinger campanha.space
# hPanel → Contas FTP → copie usuário COMPLETO (ex: u176739135.campanha2026)
FTP_HOST=77.37.127.105
FTP_USER=u176739135.campanha2026
FTP_PASS=sua_senha_ftp
# IMPORTANTE: site ao vivo serve da raiz FTP "/", NÃO de /public_html
FTP_REMOTE_DIR=/
FTP_API_DIR=/api

# Supabase → Project Settings → API → service_role (secret)
SUPABASE_SERVICE_ROLE_KEY=
