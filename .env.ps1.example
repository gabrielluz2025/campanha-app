# Copie para .env.ps1 e coloque a senha FTP/SSH do Hostinger
# Hostinger > Sites > campanha.space > Arquivos > Contas FTP > Alterar senha FTP
# (a mesma senha funciona no deploy automatico via SSH porta 65002)
$env:FTP_PASS = "sua-senha-ftp-aqui"
